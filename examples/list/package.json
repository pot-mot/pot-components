{
    "name": "@potmot/list-example",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "type-check": "vue-tsc --build --force",
        "dev": "vite",
        "build": "vue-tsc -b && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@potmot/list": "workspace:*"
    }
}
